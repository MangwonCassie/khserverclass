<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
	<entry key="loginMember">
		SELECT *
		FROM MEMBER
		WHERE USER_ID = ?
		AND USER_PWD = ?
		AND STATUS = 'Y'	
	</entry>
	
	<entry key="insertMember">
		INSERT INTO MEMBER(	 USER_NO
							,USER_ID
							,USER_PWD
							,USER_NAME
							,PHONE
							,EMAIL
							,ADDRESS
							,INTEREST)
					VALUES(SEQ_UNO.NEXTVAL
						   ,?
						   ,?
						   ,?
						   ,?
						   ,?
						   ,?
						   ,?)	
	</entry>
	
	<entry key="updateMember">
		UPDATE MEMBER
		SET USER_NAME = ?
			,PHONE = ?
			,EMAIL = ?
			,ADDRESS = ?
			,INTEREST = ?
			,MODIFY_DATE = SYSDATE
		WHERE USER_ID = ?
	
	</entry>
	
	<entry key="selectMember">
		SELECT *
		FROM MEMBER
		WHERE USER_ID = ?
		AND STATUS = 'Y'
	</entry>
	
	
	<entry key="updatePwd">
		UPDATE MEMBER
		SET USER_PWD = ?
		WHERE USER_ID = ?
		AND STATUS = 'Y'
	</entry>
	
	<entry key="deleteMember">
		UPDATE MEMBER
		SET STATUS = 'N'
		WHERE USER_ID = ?
		AND USER_PWD = ?
		AND STATUS = 'Y'
	</entry>
	
	<!-- 만약에 USER_ID에 해당하는게 있으면 그냥 1이 COUNT되고, 없으면 COUNT가 안되서 0이 반환됨 RSET 이런 거 안 쓰려고 이렇게 씀  -->
	<!-- 별칭도 부여할 것 -->
	<entry key= "checkId">
	SELECT COUNT(*) "COUNT"
	FROM MEMBER
	WHERE USER_ID = ?
	</entry>
	
</properties>